!> ベクトル演算テストモジュール
module mod_monolis_vec_util_test
  use mod_monolis_utils
  implicit none

contains

  subroutine monolis_vec_util_test
    implicit none

    !call monolis_vec_copy_I(n, ndof, X, Y)
    !call monolis_vec_copy_R(n, ndof, X, Y)
    !call monolis_vec_copy_C(n, ndof, X, Y)
    !call monolis_vec_AXPBY_I(n, ndof, alpha, X, beta, Y, Z)
    !call monolis_vec_AXPBY_R(n, ndof, alpha, X, beta, Y, Z)
    !call monolis_vec_AXPBY_C(n, ndof, alpha, X, beta, Y, Z)
  end subroutine monolis_vec_util_test

end module mod_monolis_vec_util_test
