!> 線形ソルバテストモジュール
module mod_monolis_solve_test
  use mod_monolis

  implicit none

contains

  subroutine monolis_solve_test
    implicit none

    !call monolis_solve(monolis, B, X)
    !call monolis_set_RHS(monoMAT, B)
    !call monolis_set_initial_solution(monoMAT, X)
    !call monolis_get_solution(monoMAT, X)
    !call monolis_solve_main(monoPRM, monoCOM, monoMAT, monoPREC)
    !call monolis_solver(monoPRM, monoCOM, monoMAT, monoPREC)
    !call monolis_timer_finalize(monoPRM, monoCOM)
  end subroutine monolis_solve_test

end module mod_monolis_solve_test
